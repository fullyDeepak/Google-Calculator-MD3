Feature will be added in future:

support for os level dark mode
implement the logic for delete the content at caret(cursor)
implement touch effect while pressing on keyboard


fixed:
implement the logic for the operator so can only input one at a position (no more multiple +,-,*,/)
at the time of evalation if input field has any operator at last then truncate it.
implement the logic for ()

